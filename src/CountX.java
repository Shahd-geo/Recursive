public class CountX {
    public static int countX(String str) {
        if (str.isEmpty()){
            return 0;
        }
        char fristchr=str.charAt(0);
        String REMAINING = str.substring(1);
        if (fristchr=='x'){
            return
        }

    }
}
